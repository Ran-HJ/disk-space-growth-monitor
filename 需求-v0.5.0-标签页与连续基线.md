# 功能需求：标签页重构 + 连续基线 + 盲区提示（v0.5.0）

> 提交人：用户实测反馈（基于 v0.4.0）
> 评审：Codex 两轮评审通过（8.5 → 9.5/10）
> 状态：**定稿，可进入开发**（第二轮 6 条实现细节已并入，见第四节 9-14）

---

## 一、三个标签页（主区域重构）

主区域改为 `ttk.Notebook` 三个标签页：

| 标签 | 内容 |
|---|---|
| **① 空间分布** | treemap + 导航（↑ 上一级、面包屑、扫描路径、开始/取消扫描、保存快照）+ **最近 24h 趋势图**（迁入本页） |
| **② 空间变化** | 增长/减少切换 + 基线信息行 + 变化总行（快照口径）+ 盲区行（磁盘口径）+ 增长树（现有树形去重复用）+ 「恢复默认基线」按钮 |
| **③ 快照历史** | 快照列表（游标分页）+ 手动快照标记高亮 + **双快照对比选择入口（唯一入口）** |

### 布局约束

- **标签页之外全局常驻**：顶部磁盘总容量 / 已使用 / 可用空间 / 本次启动变化 四张指标卡，以及底部状态栏，保持在 Notebook 之外，切换标签不隐藏。
- 趋势图归属 ① 页。
- 扫描控制归属 ①；② 页只读展示；③ 页选择「指定对比」后切换到 ② 显示结果。
- **交互联动（实现细节 #9）**：在 ② 页增长树双击目录钻取后，**自动切换到 ① 页**展示新的 treemap（否则导航完成但用户仍停留在只读变化页，看不到结果）。
- 增长树、`build_growth_tree`、双击钻取、缓存导航等现有逻辑**原样复用**，只改布局归属。

---

## 二、基线固定规则（无用户选择项）

启动自动扫描照旧执行，得到本次快照 C：

```
判定：最近一次已完成会话（status != 'active' 的最新 monitor_sessions 行），且满足全部条件：

├─ end_reason = 'normal_close'
│   AND end_snapshot_id 非空
│   AND root_path 与本次启动路径相同（实现细节 #10：不同路径不得使用其关闭快照）
│     → 基线 = 该 end_snapshot_id 对应快照
│     → ② 页直接对比「C vs 上次完整快照」
│     → 基线信息行：「基线：上次完整保存（YYYY-MM-DD HH:MM）」
│
└─ 任一条件不满足（quick_close / interrupted / 无 end 快照 / 路径不同 / 首次）
      → 基线 = C（本次快照即新基线，保持 v0.4.0 现状）
      → ② 页显示「已建立基线，关闭时对比」
```

### 关键点

- 判定只看最新会话；快速退出不回退更早完整快照（用户已确认）。
- 首次扫描建基线逻辑不变。
- 基线对比对象为**同路径**最近快照；不同路径不混比。
- 连续追踪效果：正常完整关闭链上，每次启动的对比都以「上次关闭快照」为原点，跨会话不断档。

---

## 三、盲区提示（未监控期间的变化）

### 触发条件

- 对比对象：**当前磁盘已用量 vs 最近一条 disk_samples 旧记录**（磁盘口径 `used_bytes`，与快照 `total_bytes` 文件口径严格区分）。
- 阈值：差值绝对值 **> 100 MB**（写死；可配置化为后续方向）。
- 增减**都报**：增长 = 橙，减少 = 绿。
- **判定时机：仅启动后首次采样判定一次**；运行中每分钟采样不重复判定。

### 计算顺序（写死 —— 本需求最容易产生 Bug 的地方）

启动首次采样必须严格按以下顺序执行：

```
1. 读取数据库最后一条旧 disk_samples 记录（用于盲区计算与文案时间戳）
2. 获取当前磁盘用量 sample
3. 计算盲区差值（sample.used_bytes - 旧记录.used_bytes），判定是否 > 100MB
4. 执行 30 天清理（prune_disk_samples）
5. 写入本次采样（add_disk_sample）
```

⚠️ 禁止事项：
- 先写入本次采样再计算 → 差值恒为 0；
- 先执行 30 天清理再读取旧记录 → 超过 30 天未运行时最后一个比较点被清理。

### 显示（② 页，两种口径分开两行）

| 行 | 口径 | 颜色 | 文案 |
|---|---|---|---|
| 文件快照变化 | 文件逻辑大小 | 增长红 / 减少蓝 | `+1.2 GB`（自基线的目录级对比） |
| 未监控期间磁盘变化 | 磁盘已用容量 | 增长橙 / 减少绿 | `⚠ 期间未监控 X 天，变化 +350 MB（无目录明细）` |

- **两行禁止相加**（口径不同）。
- X 天 = 当前时间 − **旧记录**时间戳（不足 1 天显示小时）。
- **无旧采样时（实现细节 #14）**：首次使用或数据库无旧记录 → 显示「无上次采样，无法判断未监控期间变化」，**不显示 0，不触发警告**。
- 与「其他未展开变化」语义不同，文案/颜色可区分。

---

## 四、实现约束与细节（两轮评审全部并入）

### 第一轮（评审 #1-7）

**1. 两种口径禁止混算**：快照差异（文件逻辑大小）与盲区（磁盘已用）分开显示、分开统计、禁止相加。

**2. 盲区计算顺序写死**：见第三节「计算顺序」。

**3. 快照来源分类**：
- `snapshots` 表新增 `source TEXT` 列（迁移：`PRAGMA table_info` 检测 + `ALTER TABLE ADD COLUMN`，旧库自动升级）。
- 写入时机：`save_scan` 调用处按角色传入：`baseline` / `closing` / `manual` / `navigation` / `manual_save`。
- **显示时引用关系优先（实现细节 #12）**：一个快照可能保存时是普通/手动，后来被用作会话基线。历史页来源判定顺序统一为：
  `先查 monitor_sessions.start_snapshot_id / end_snapshot_id 引用 → 再看 source 字段`
  最终优先级：**关闭快照 > 启动基线 > manual_save > manual / navigation**。旧记录与新记录使用同一套判定。

**4. 双快照对比限制**：同路径、不同快照、自动按时间定旧新、显示「新快照时间 − 旧快照时间」、异路径禁用并提示。

**5. 历史列表分页（游标分页，实现细节 #13）**：
- 首批 200 条，用 **keyset 游标分页**：`WHERE id < 上一页最后一条快照的 id ORDER BY id DESC LIMIT 200`，不用 `OFFSET`（扫描期间新增快照不会造成重复或漏项）。

**6. 对比入口收敛**：③ 页是唯一历史选择入口；② 页只显示「当前比较对象」+「恢复默认基线」按钮（回自动基线状态，不是清空数据）。

**7. 标签页外元素常驻**：顶部指标卡 + 底部状态栏全局可见；趋势图放 ① 页。

### 第二轮（实现细节 #9-14，非阻塞但必须遵守）

**8.（#9）② 页双击钻取后自动切到 ① 页**：见第一节交互联动。

**9.（#10）基线条件含路径匹配**：`normal_close + end_snapshot_id 非空 + root_path 与本次启动路径相同`，见第二节状态机。

**10.（#11）快照排序处理同秒记录**：`finished_at` 保存到秒，两个快照可能同秒。统一使用
`ORDER BY finished_at DESC, id DESC`；新旧判定以 `(finished_at, id)` 元组为准（列表显示、双快照对比、分页均适用）。

**11.（#12）来源判定引用优先**：见约束 #3。

**12.（#13）游标分页**：见约束 #5。

**13.（#14）无旧采样的盲区显示**：见第三节显示表。

**14. 补充约束（宝宝第一轮补充）**：盲区判定仅启动首次采样一次；盲区「X 天」基于旧记录时间戳；「恢复默认基线」= 回自动基线状态。

---

## 五、回归注意

- 现有 22 项测试全绿为前提；标签页重构后 `test_ui_flow.py` 需同步更新。
- 导航缓存、单实例锁、关闭行为、90 天快照保留、手动快照备注等 v0.4.0 行为**全部不变**。
- 盲区判定只影响 ② 页显示，不改变采样、扫描、会话落库逻辑。
- `snapshots` 表加 `source` 列后，`load_snapshot` / `list_marked_snapshots` 等查询需兼容旧行（source 为 NULL 时按引用关系推导）。

## 六、后续优化方向（仅记录，本期不做）

- 盲区阈值 100MB 改为可配置（设置页）。
- 快照历史页：按路径过滤、导出对比报告。

## 七、验收标准

1. 三个标签页可切换，① treemap + 趋势图布局正常，②③ 功能完整。
2. 顶部指标卡与底部状态栏在切换标签时保持可见。
3. ② 页双击目录钻取后自动切到 ① 页并展示新 treemap。
4. 连续场景：完整关闭（同路径）→ 下次启动 → ② 显示「基线：上次完整保存（时间）」+ 快照口径变化，无断档。
5. 路径不同场景：上次完整关闭是其他路径 → 本次启动建立新基线，不误用旧路径快照。
6. 快速退出场景：下次启动 → ② 显示「已建立基线，关闭时对比」。
7. 盲区场景：模拟 >100MB 间隔变化 → ② 出现「⚠ 未监控 X 天，变化 +X（无目录明细）」，增长橙 / 减少绿；两行分开、不相加。
8. 无旧采样场景：全新数据库 → 显示「无上次采样，无法判断未监控期间变化」，无 0、无警告。
9. 盲区顺序回归：先写采样或先清理的写法必须不出现（代码审查 + 单测覆盖）。
10. 快照历史：来源显示引用优先（关闭 > 基线 > 手动 > 普通），同路径双快照对比可用（自动定新旧），异路径禁用并提示，游标分页 200 + 加载更多无重复/漏项。
11. 首次使用：首扫后显示「已建立基线」，与 v0.4.0 一致。
