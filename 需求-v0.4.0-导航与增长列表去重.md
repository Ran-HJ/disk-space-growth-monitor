# 功能需求：导航历史 + 增长列表去重 + 手动保存快照（v0.3.0 → v0.4.0）

> 提交人：用户实测反馈（基于 v0.3.0）
> 优先级：P0 = 导航历史（体验断点），P1 = 增长列表去重（信息冗余），P2 = 手动保存快照（增强）

---

## 背景（用户实测痛点）

1. **只能向下钻取，不能向上返回**：双击 treemap 进入子目录后，想回上一级只能重新输入路径再扫一遍。
2. **下一级扫描结果没有短暂保存**：从子目录返回后，再想回子目录看，又要重新扫。
3. **增长来源列表同一增长重复显示**：截图实测 `c:\ +13.4MB → Users +13.4MB → ranhejian +13.4MB` 是同一增长的三级重复；`Windows +17.4KB → System32 +17.4KB` 同理。用户希望「一个文件只显示一个」。

期望交互参考：**SpaceSniffer**——点击「向上」返回上一级；下钻过的层级短暂保留，可随时往返；层级切换是视口/缓存导航，不重新扫描。

---

## P0 — 导航历史（向上返回 / 向下回访）

### 现状

- `ui.py` 的 `_open_rectangle`（treemap 双击）与 `_open_growth_item`（增长列表双击）都只做 `path_var.set(路径) + _start_scan()`，无任何历史记录。
- **利好**：`_scan_worker` 每次扫描完成都调用 `storage.save_scan(result)`，所有层级扫描（含钻取）都已持久化到 `snapshots` / `snapshot_items` 表，且保留 90 天。**向上返回的「短暂保存」数据天然已经存在**，只缺读取与导航层。

### 建议实现

1. **新增 `Storage.load_snapshot(snapshot_id) -> ScanResult | None`**：从 `snapshots` + `snapshot_items` 重建 `ScanResult`（含 items 列表），供导航直接渲染。需要处理 `ScanResult.items` 的排序（按 path）与 `snapshot_id` 回填。
2. **UI 导航栈**：
   - `self.nav_stack: list[str]` 记录当前路径链（如 `["C:\\", "C:\\Users", "C:\\Users\\ranhejian"]`）。
   - 双击钻取 → push；「↑ 上一级」→ pop 并渲染上级路径。
   - 渲染逻辑 `_navigate_to(path, scan: bool)`：
     - 优先查**本次会话已扫描的路径缓存**（`self.nav_cache: dict[path, ScanResult]`，内存即可）；
     - 缓存未命中则查 **DB 最近一次该 root_path 的快照**（`SELECT id FROM snapshots WHERE root_path = ? ORDER BY id DESC LIMIT 1`）；
     - 都未命中才真正重新扫描（并自动写入缓存与 DB）。
   - 界面加「↑ 上一级」按钮（放在扫描路径输入框旁），可用时 enabled。
3. **缓存一致性**：
   - 缓存项记录扫描时间，状态栏/详情栏可显示「数据时间」；
   - 保留「重新扫描当前目录」按钮（刷新该层缓存，同时触发与上一快照的增长对比）；
   - 导航（纯缓存渲染）**不改变**增长对比上下文；显式重新扫描才更新对比。
   - 内存缓存生命周期 = 本次会话（`nav_cache` 会话级，退出即清；DB 快照按现有 90 天策略保留）。
4. **可选增强（若顺手）**：路径**面包屑**（`C:\ > Users > ranhejian` 可点击跳转），比 SpaceSniffer 的单按钮更好用。

### 验收标准

- 双击进入子目录 → 点「↑ 上一级」立即返回（无需重新扫描、无进度条卡顿）；
- 返回后再向下进入同一子目录：若未重新扫描过，直接渲染旧快照（秒开）；
- 重新扫描某层后，该层及以下缓存失效并用新数据渲染；
- 会话重启后向上返回走 DB 快照仍可用（90 天内）。

---

## P1 — 增长来源列表去重（一个变化只显示一次）

### 现状

- `storage.compare_snapshot_changes` / `get_session_growth` 把**全部层级**（record_depth=2 的目录 + top200 文件）的变化合并进一个平铺列表，父子目录同链重复显示（`c:\`、`Users`、`ranhejian` 三个条目同 +13.4MB）。
- 截图见附件：同一增长最多出现 3 次（根/一层/二层），信息冗余且列表被稀释。

### 建议实现（二选一，推荐 A）

**方案 A（推荐）：增长列表改为层级树（Treeview tree 模式）**
- `growth_tree` 从 `show="headings"` 改为 `show="tree headings"`，列仍为（项目 / 当前 / 增长）。
- 构建层级：`c:\`（根）为一级节点，`Users`、`Windows` 为其子节点，`ranhejian`、`System32` 为二级子节点……父节点行**显示子节点汇总增长**（或显示链上最深层明细）；用户点击展开/折叠。
- 「增长」列只在**叶子/明细行**显示，避免重复感；父行可显示「(含子项共 X)」。
- 双击叶子目录行 → 复用 P0 导航钻取。
- 注意：`GrowthItem` 需要补 `parent_path` 字段以构建树（models.py 已无此字段，需加；`session_growth_items` 表已有 path，可从 path 推导父链）。

**方案 B（轻量）：SQL 层折叠同链变化**
- 对增长项按「变化量相同且路径互为父子」做折叠，只保留最深层；`ORDER BY` 不变。
- 优点改动小；缺点丢失「哪个大目录整体涨了」的汇总视角，且折叠逻辑在 SQL 里较绕。
- 若选 B，建议至少保留根行（总变化）一行汇总。

### 验收标准

- 同一增长链（如 Users 13.4MB = ranhejian 13.4MB）在列表中**只出现一次**（方案 A：叶子行显示一次，父行仅汇总）；
- 展开/折叠正常，双击叶子目录可钻取；
- 增长/减少视图切换后树结构同样生效。

---

## P2 — 手动保存快照（可选增强）

### 需求

用户希望「自己选择保存当前快照」——即对某个关键时刻（如清理前后）的目录状态做显式标记快照，便于日后对比。

### 建议实现

1. 工具栏加「保存快照」按钮：对当前路径执行一次扫描并落库（复用现有 `save_scan`），弹窗允许填备注。
2. `snapshots` 表加 `note TEXT` 列（可空，兼容旧行）；有备注的快照在增长对比时可选「与指定快照对比」（下拉选择）。
3. **重要约束（防回归）**：自动快照（会话基线、关闭快照、每次扫描落库）是增长对比的数据源，**不能因手动功能而关闭或减少**——手动保存只是额外的「标记点」，二者并存。

### 验收标准

- 手动保存后 DB 出现带备注的新快照，90 天保留规则同样适用；
- 自动快照行为与 v0.3.0 完全一致（回归测试覆盖）。

---

## 实现顺序建议

1. `Storage.load_snapshot` + 导航栈 + 「↑ 上一级」按钮（P0，约半天）
2. 增长列表树形化（P1，约半天～1 天）
3. 手动保存快照（P2，半天）

## 回归注意

- 现有 13 项测试全绿为前提；新增 `load_snapshot` 后补存储层测试（重建 ScanResult 的 items 排序、snapshot_id 回填）。
- 导航/缓存不影响：会话基线、关闭快照、`finish_session`、单实例锁等现有行为。
