# 功能需求：v0.6.2 修复与体验补充

> 提交人：用户实测反馈（基于 v0.6.1 视觉验收）
> 关联：v0.6.0 验收失败后由 v0.6.1 修复；本批为视觉验收发现的问题
> 状态：已开发，待用户验收

---

## 一、基线模式可手动切换（② 空间变化页）

### 现状问题

- 基线只有「恢复默认基线」一个入口，恢复后无法再手动切到另一种基线。
- 用户期望：检测基线可以是 **「重启时的新基线」** 或 **「最近一次完整记录」**，二选一，可来回切换。

### 需求

1. ② 页基线区域增加**基线模式选择**（RadioButton 或下拉）：
   - 「重启新基线」：以本次启动快照为对比原点（现有"已建立基线，关闭时对比"逻辑）。
   - 「最近完整保存」：查找最近一次同路径、正常完整关闭且 `end_snapshot` 非空的快照；允许越过其后的快速关闭会话。**仅当存在符合条件的完整快照时显示该选项**，否则只显示「重启新基线」。
2. 切换立即生效：重新计算当前对比结果并刷新增长树/变化行，不要求重新扫描。
3. 「恢复默认基线」按钮保留：回到 v0.5.0 自动判定规则（有完整保存用完整保存，否则新基线）——恢复后用户仍可通过上述选择器再切。
4. 基线信息行同步更新（如「基线：本次启动快照」「基线：上次完整保存 (时间)」）。
5. 手动选择仅在当前程序会话中有效；重启后仍按连续基线自动规则重新判定。自定义历史比较和子目录比较不伪装成会话基线。

### 实现提示

- 现有 `continuous_baseline_snapshot_id()` 已提供判定逻辑；新增手动模式时，把「当前基线」从"自动判定结果"扩展为"用户选择的模式 + 自动判定结果"，切换只改上下文引用，不动存储。

---

## 二、运行中磁盘增长对账（② 空间变化页）

### 现状问题（用户截图反馈）

- 顶部指标卡显示「本次启动后变化 +223.3 MB」（磁盘口径，启动采样 → 最新采样）。
- 但 ② 页只有「文件快照变化 0 B」（仅基线快照，无第二次扫描）+「未监控期间磁盘变化 +22.1 MB」（启动前的盲区）。
- **运行中的 +223.3 MB 在 ② 页无对应展示**，增长对不上账，用户无法判断"磁盘涨了，具体涨在哪"。

### 需求

1. ② 页变化区新增一行 **「本次运行期间磁盘变化（磁盘口径）」**：启动采样 → 最新采样 的 `used_bytes` 差值（就是顶部卡片同源数据）。
2. 该行数值为 0 时显示 `0 B`（灰）；非 0 时：
   - 颜色与盲区行一致（增长橙 / 减少绿）。
   - 文案追加引导：**「具体文件位置需完成一次扫描后定位」**——提示用户点击「重新扫描当前目录」即可立即得到明细对比（扫描完成后增长树刷新，账目对上）。
3. 与既有两行（文件快照变化 / 未监控期间磁盘变化）并列，**三者口径各自标注、禁止相加**（延续 v0.5.0 约束）。
   - 同时显示各自时间范围和文件快照的路径范围；磁盘已用量与文件逻辑大小不承诺严格相等。
4. 场景说明：
   - 关闭时完成快照对比后，「本次运行期间磁盘变化」与「文件快照变化」应同方向接近（口径差异允许，但不应矛盾）；若差异显著，维持现有"文件逻辑大小 vs 磁盘已用"的说明文案即可。

---

## 三、快照对比支持任意两项（③ 快照历史页）

### 现状问题

- 对比选择只能选**相邻**两项（列表连续选择限制），无法对比跨多个条目的两个快照。

### 需求

1. ③ 页对比交互改为**任意两项**：
   - 普通单击选中第一个快照（高亮）→ 普通单击选中第二个快照；再次单击已选快照取消选择。
   - 保留「对比」按钮，选中恰好两项后点击触发，避免误操作；Ctrl/Shift 仍保留系统多选行为。
2. 保留既有约束（v0.5.0 评审 #4）：
   - 只能选择**相同路径**的两个快照；不同路径禁用对比并提示原因。
   - 不能选择同一快照。
   - 自动按时间确定「旧 → 新」，不依赖点击顺序。
   - 对比结果显示「新快照时间 − 旧快照时间」。
3. 选中逻辑放开后，列表多选行为（Ctrl/Shift）与新交互不冲突即可。

---

## 四、可靠性加固（Codex v0.6.1 报告建议，并入本轮）

> v0.6.0 失败教训：EXE 冒烟测试只确认了启动/关闭/落库，未等待基线及矩形图完成，导致打包版真实扫描时的阻断问题漏检。本轮一并补齐。

### 4.1 界面回调异常写入日志
- 现状：打包版（无控制台）下，UI 回调（绘制、刷新、采样、消息处理）中的异常可能静默失败，用户只看到"空白/无反应"，无法定位。
- 需求：UI 层关键回调（treemap 绘制、趋势绘制、增长树刷新、`_poll_messages`、`_sample_now`、扫描收尾）统一 try/except，异常写入日志文件（`%LOCALAPPDATA%\DiskGrowthMonitor\ui.log` 或现有日志机制），并尽量在状态栏显示可读错误。

### 4.2 绘图异常时扫描状态正确收尾
- 即使 treemap/趋势图绘制抛异常，也保证：扫描按钮恢复可用、进度条停止、基线状态正确更新、取消/关闭流程正常。
- 绘制失败不得影响扫描数据落库与会话流程。

### 4.3 EXE 冒烟测试等待基线及矩形图完成
- `smoke_exe.ps1`（或等价脚本）升级：启动后**轮询等待启动基线快照完成且 treemap 非空**（可检查 DB 的 start_snapshot_id + 窗口截图/像素采样），不再只确认"进程启动 + 关闭 + 落库"。
- 该测试应能捕获 v0.6.0 类问题（扫描完成但矩形图为空、基线未建立）。

### 4.4 修订 PROGRESS.md
- 记录：v0.6.0 验收失败原因（矩形图空、基线未建立、骨架未建立、变化无法展示）、v0.6.1 修复内容（多顶层项目矩形图修复、回归场景补充）、v0.6.2 修复内容（基线切换/对账/任意对比/可靠性加固）。
- 修正现有 "v0.6.0 验证通过" 的不准确记录。

---

## 五、回归注意

- 现有 36 项测试全绿为前提；新增/修改逻辑补测试：
  - 基线模式切换的状态与计算（storage/service 层纯逻辑部分可单测）。
  - 快照对比选择约束（同路径/非同一/时间排序）。
- v0.6.1 已验收行为不变：连续基线、盲区提示、秒开导航、标签页、关闭行为、骨架内存。
- 骨架内存口径已校正：50 MB 是文件明细软预算，目录节点为秒开导航的必要数据；底部另报含目录节点的骨架估算值。v0.6.2 不盲目提高到 150 MB。隔离环境真实 C 盘扫描实测窗口进程峰值 185.4 MB、单文件 EXE 两进程合计峰值 195.7 MB，后续再评估目录节点压缩或惰性加载。

## 六、验收标准

1. ② 页可手动切换「重启新基线」/「最近完整保存」，来回切换生效且无需重扫；无上次完整保存时只显示「重启新基线」。
2. 「恢复默认基线」后仍可通过选择器再切。
3. ② 页显示「本次运行期间磁盘变化」行：非 0 时橙色/绿色 + 「具体文件位置需完成一次扫描后定位」引导；扫描后增长树刷新，与顶部卡片口径一致。
4. ③ 页可对比任意两个非相邻快照（同路径约束保留），自动定新旧、显示时间差。
5. 可靠性：制造绘制异常（可注入）后，扫描按钮/进度条/基线状态仍正确收尾，异常写入日志文件。
6. `smoke_exe.ps1` 升级后能等待基线及矩形图完成（对 v0.6.0 类问题应能检测失败）。
7. PROGRESS.md 已记录 v0.6.0 失败原因与 v0.6.1/v0.6.2 修复结果，旧的不准确记录已修正。
8. 39 项测试结果 OK：38 项通过，1 项因本机符号链接权限跳过。
