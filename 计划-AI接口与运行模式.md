# 计划文档：AI 操作接口 + 运行模式切换（想法收集）

> 提交人：用户（2026-08-19）
> 状态：**已排期**（版本路线见第四节，2026-08-19 更新）；各版本独立需求文档另附

---

## 一、AI 操作接口（"AI 后门"→ 正名：本地自动化接口）

### 目标

让 AI 助手（Hermes / Claude / Codex）与自动化脚本能直接**检测**（查询数据）与**操作**（触发扫描等）本工具，无需人工开界面。

### 阶段 1：CLI 命令行（v0.7.1，只读优先）

**第一批（只读，v0.7.1 交付）**：

```
diskmonitor status              → C盘 142.2GB/199GB · 本次 +223MB · 当前模式
diskmonitor growth last         → 上次会话/最近快照对比增长 Top10（路径+大小）
diskmonitor snapshot list       → 快照清单（时间/来源/备注，游标分页）
diskmonitor report export       → 导出增长报告（CSV/Markdown）
所有命令支持 --json             → 方便 Codex/Claude/Hermes 调用
```

**第二批（写操作，GUI 并发归属规则解决后再开放）**：
`scan <path>` / `snapshot save <note>` / `mode set` —— 需先定义 GUI 同时运行时的操作归属与并发规则。

- 实现：argparse + 复用现有 `Storage` / `service` / `scanner` 层，纯本地进程调用，零网络面。
- 配套：写一个 Hermes skill，让 Hermes 能直接回答"C 盘最近谁涨得多"。
- 风险：低。不改变 GUI 行为。

### 阶段 2：MCP Server（统一接口，后续）

- 在 CLI 逻辑上包一层 MCP server（fastmcp/mcp 库），工具：`disk_status`、`disk_growth`、`disk_scan`、`snapshot_list`、`snapshot_save`。
- Claude Desktop / Claude Code / Hermes 均可接入。
- 只监听 localhost；只读为主；触发类操作（扫描/快照）均为安全操作。

### 安全边界（写死）

- 不暴露网络端口（CLI 进程内 / MCP 仅 localhost）。
- 永不提供删除/修改文件能力（与产品"只读分析"原则一致）。

---

## 二、运行模式切换（低内存 / 全功能）

### 背景

笔记本（16GB 内存）打游戏（Apex / CS2）时需要省内存；平时无压力可全功能。

### 两种模式

| 模式 | 保留 | 砍掉 |
|---|---|---|
| **全功能模式**（默认） | 一切现有功能：自动基线扫描、目录骨架、导航、增长对比、快照历史 | — |
| **低内存模式** | 仅**磁盘容量分钟采样**（disk_samples）+ 趋势图 + 顶部指标卡 | 自动基线扫描、目录骨架、导航缓存、快照扫描、增长对比（暂停） |

- 低内存模式内存占用目标：**根据第四节三场景实测结果确定，不预先承诺 <50 MB**（当前骨架就 115MB，砍掉后应显著下降）。
- 低内存模式下关闭 = 快速退出语义（只记总容量，不扫关闭快照）；切回全功能后再补扫。
- v0.7.0 只通过**设置页手动切换**；托盘与托盘菜单延后到 v0.7.2。

### 切换方式

1. **手动**：设置页选择（v0.7.0 交付；托盘菜单随托盘一起到 v0.7.2）。
2. **自动**（待定方案）：
   - 方案 A：**前台进程检测**——用户配置游戏清单（如 Apex、CS2），检测到游戏为前台/运行中 → 自动切低内存；退出游戏 → 自动切回全功能（并提示补扫）。
   - 方案 B：**内存压力检测**——系统内存占用 > 阈值（如 85%）→ 自动切低内存；回落后再切回。
   - 方案 C：定时（如 22:00-02:00 默认低内存）——灵活度最低，优先级最低。
   - 推荐：A + B 结合（游戏清单为主，内存压力兜底）。

---

## 三、宝宝补充的想法（讨论中）

1. **游戏结束后"补扫"闭环**：低内存期间只记容量 → 切回全功能时提示「游戏期间增长 X MB，未定位明细，是否补扫？」——与现有盲区提示逻辑天然衔接，账目不断。
2. **低内存模式对笔记本的额外收益**：磁盘 IO 大幅减少 → 更省电、更安静、游戏帧数更稳（背景扫描少占 IO 带宽）。
3. **CLI 与模式联动**：CLI 在任何模式下可查询；低内存模式下 CLI 触发扫描 = 临时执行一次完整扫描（不切换模式）。
4. **开机自启可选模式**：自启参数支持 `--low-memory`（如"默认全功能，但开机自启用低内存"配置项）。
5. **模式切换不影响会话连续性**：低内存期间会话仍 active，切回全功能后正常建档/关闭对比（只是中间段无明细，走盲区提示）。

---

## 四、版本路线（2026-08-19 更新，按检查点拆分）

> 由 Codex 建议重排：不把大功能一次塞进一个版本，每版一个核心目标、明确验收点。

| 版本 | 核心目标 | 主要验收点 | 状态 |
|---|---|---|---|
| **v0.6.3** | 关闭日志补丁 | 完整关闭出现 `scan_ui_finished role=closing`；冒烟扩展为完整关闭全链路 | 已开发并自动验证，待用户验收 |
| **v0.7.0** | 手动低内存模式 | 不扫描、不保留骨架，可安全切回全功能；详见《需求-v0.7.0-手动低内存模式.md》 | 计划 |
| **v0.7.1** | AI/脚本 CLI | 只读：`status` / `growth last` / `snapshot list` / `report export`（全部支持 `--json`）；写操作（scan/snapshot save/mode set）后续解决 GUI 并发归属后再开放 | 计划 |
| **v0.7.2** | 自动模式切换 | 游戏检测、内存压力、托盘及补扫闭环 | 计划 |
| **v0.8.0** | MCP Server | 基于稳定 CLI 封装本地 AI 工具（Claude/Hermes 可接） | 计划 |

### v0.7.0 排期理由（Codex）

- 优先于 CLI：真实 C 盘扫描测得窗口进程峰值 185.4 MB；运行模式是 CLI 的基础数据模型，先做稳状态转换。
- 第一版只做手动切换：自动检测、托盘、启动参数均延后。

### 版本间依赖

- v0.6.3 必须先完成（验收通过后进入 v0.7.0）。
- v0.7.0 为 v0.7.2 提供模式状态机基础；v0.7.1 为 v0.8.0 提供稳定 CLI。

## 五、记录来源

- 用户随手想法（2026-08-19）：AI 操作接口、低内存/全功能模式、自动切换。
- 宝宝补充：补扫闭环、省电收益、CLI 联动、自启参数、会话连续性。
- Codex 版本规划（2026-08-19）：按检查点拆分版本路线，v0.7.0 只做手动低内存模式。
