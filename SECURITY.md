# 安全策略

## 支持范围

| 版本 | 安全报告 |
| --- | --- |
| `main` / 当前 v0.8.x 源码 | 支持 |
| 最新 GitHub Release v0.7.5 | 支持 |
| v0.7.4 及更早版本 | 不再单独修复；若问题仍影响当前版本，请继续报告 |

## 私密报告漏洞

请使用 GitHub 的 [Private Vulnerability Reporting](https://github.com/Ran-HJ/disk-space-growth-monitor/security/advisories/new) 提交安全问题。不要为未修复漏洞创建公开 Issue。

报告建议包含：

- 受影响版本或提交；
- 风险、影响范围和必要前置条件；
- 使用临时目录或隔离数据的最小复现步骤；
- 可行的缓解或修复建议；
- 可以公开致谢的名称（可选）。

维护者会尽力确认、复现和评估报告，但不承诺固定响应时间或远程支持。报告被接收为 Security Advisory 不会自动公开；是否以及何时公开由修复和协调披露进度决定。

## 隐私要求

即使使用私密报告，也请只提供复现所需的最少信息。不要提交：

- 密钥、令牌、密码或 `control-*.auth`；
- 完整 `monitor.db`、迁移备份或其他正式数据库；
- 未脱敏的日志原文、用户名和个人路径；
- 命名管道地址、认证材料或无关的系统信息；
- 包含真实个人文件内容的截图或附件。

请优先使用 `TemporaryDirectory`、临时 SQLite 数据库和自建夹具复现。若问题只能在真实环境出现，请先描述现象和最小元数据，由维护者确认下一步所需材料。

## 适合报告的范围

- 本地控制认证或权限绕过；
- 数据库迁移导致的数据损坏或越权写入；
- 路径、日志、诊断或错误输出泄露敏感信息；
- 扫描器意外修改用户文件；
- 构建、依赖或发布附件的供应链风险；
- 其他影响机密性、完整性或可用性的缺陷。

本项目不提供远程运维服务，也不承诺自动删除、修复、移动或恢复用户文件。普通功能缺陷和改进建议请使用脱敏后的公开 Issue 模板。
