# C 盘空间增长监控器验收清单

建议先用一个测试目录验证增长追踪，再进行完整 C 盘扫描。

## 1. 启动检查

1. 双击 `dist\C盘空间增长监控器.exe`。
2. 确认窗口能正常打开，没有命令行黑框。
3. 确认顶部显示“磁盘总容量、已使用、可用空间、本次启动后变化（磁盘口径）”。

预期结果：程序启动后显示当前磁盘容量，并自动开始建立启动目录基线；界面可拖动、缩放且内容不被裁切。

## 2. 普通目录扫描

1. 点击“选择目录”，选择一个文件较少的目录。
2. 点击“重新扫描当前目录”。
3. 扫描期间观察底部状态，尝试拖动窗口。
4. 扫描结束后检查“空间分布”矩形图。

预期结果：界面扫描期间保持响应；扫描结束后，大文件或大目录占据更大的矩形。

## 3. 增长追踪

1. 新建一个专用测试目录，例如用户目录下的 `DiskMonitorTest`。
2. 对空目录执行第一次“重新扫描当前目录”，建立基线。
3. 向目录复制一个较大的文件。
4. 对同一路径执行第二次扫描。

预期结果：“空间变化来源”列表显示新增文件及其增长量。测试完成后可以手动删除测试目录，程序本身不会删除任何文件。

## 4. 会话关闭快照

1. 等待启动目录基线完成。
2. 在监控期间增加或删除一些文件。
3. 点击窗口右上角关闭按钮，选择“完整保存”。
4. 可勾选“下次不再询问”，确认后等待关闭快照完成。
5. 再次启动程序。

预期结果：关闭请求不会立即杀死程序；下次启动后，“空间变化来源”显示上一会话的累计增长量和具体路径。

## 5. 关闭设置与快速退出

1. 打开“设置”，确认关闭行为支持“每次询问、始终完整保存、始终快速退出”。
2. 选择“始终快速退出”并保存。
3. 关闭程序。

预期结果：不再显示询问框，程序快速保存总变化并退出。重新打开设置可以改回“每次询问”。

## 6. 增减视图与目录钻取

1. 扫描一个包含多个子目录的路径。
2. 在空间分布图中双击某个目录矩形。
3. 在“增长 / 减少”之间切换，并双击变化列表中的目录。

预期结果：两种变化可以分别查看；双击后开始分析该子目录。

## 7. 单实例与开机自启

1. 保持程序运行，再次双击 EXE。
2. 在设置中启用或关闭“登录 Windows 后自动启动监控器”。

预期结果：第二个实例提示程序已运行并退出；开机自启开关可以正常保存。

## 8. 取消扫描

1. 对较大的目录开始扫描。
2. 点击“取消扫描”。

预期结果：程序安全停止，不保存不完整快照，随后可以重新开始扫描。

## 9. C 盘完整扫描

1. 将路径设为 `C:\`。
2. 点击“重新扫描当前目录”。
3. 保持程序运行直至完成。

预期结果：无权限目录只增加错误计数，不会导致程序崩溃；完整扫描时间取决于文件数量和磁盘速度。

## 10. 缓存导航与面包屑

1. 扫描一个包含子目录的测试路径。
2. 双击矩形进入子目录，再点击“↑ 上一级”。
3. 再次进入相同子目录，并点击面包屑中的任意上级。
4. 点击“重新扫描当前目录”后再次进入其下级。

预期结果：已访问目录可从本次缓存或历史快照立即显示；导航本身不改变右侧变化对比；显式重新扫描后，当前层及下级旧缓存不再复用。

## 11. 增长树去重

1. 制造一条父子目录数值相同的增长链。
2. 在“增长”和“减少”之间切换并展开右侧树。

预期结果：父目录显示“含 N 项”，变化数值只在叶子出现一次；无法归属到已记录子项的差额显示为“其他未展开变化”；目录节点可以双击进入。

## 12. 手动快照

1. 点击“保存快照”，分别验证取消、空备注和正常备注。
2. 保存后修改测试目录，再点击“重新扫描当前目录”。
3. 从变化面板下拉框选择之前保存的备注快照。

预期结果：取消不保存、空备注会提示；有效备注快照可被选择并参与增长/减少对比；自动基线与关闭快照行为不受影响。

## 13. 三个标签页

1. 依次切换“空间分布、空间变化、快照历史”。
2. 检查顶部四张指标卡和底部状态栏。
3. 在空间变化页双击一个仍存在的变化目录。

预期结果：趋势图位于空间分布页；顶部指标和底部状态始终可见；变化目录双击后自动进入空间分布页并展示对应目录。

## 14. 连续基线

1. 使用“完整保存”正常关闭，再次启动同一路径。
2. 分别使用“快速退出”和异常结束后再次启动。
3. 使用不同监控路径启动进行对照。

预期结果：同路径完整关闭后显示“基线：上次完整保存”；快速、异常、首次或路径不同均建立本次新基线，不回退更早快照。

## 15. 未监控盲区

1. 在程序关闭期间制造超过 100 MB 的磁盘增长或减少。
2. 再次启动并查看空间变化页。
3. 使用全新数据库验证无旧采样场景。

预期结果：盲区增长为橙色、减少为绿色；明确标注“无目录明细”；文件快照变化单独显示且不与盲区相加；无旧采样时显示无法判断而不是 0。

## 16. 快照历史对比

1. 检查快照的时间、路径、大小、来源和备注。
2. 选择两条同路径快照进行对比，再选择两条不同路径快照。
3. 在空间变化页点击“恢复默认基线”。

预期结果：手动标记有高亮；同路径比较自动确定新旧并切换到空间变化；异路径禁用并提示；恢复按钮返回启动自动基线。

## 17. 秒开深层导航

1. 对包含三层以上子目录的路径完成一次扫描。
2. 连续双击进入未访问过的深层目录，再使用“↑ 上一级”和面包屑返回。
3. 观察底部状态和扫描进度条。

预期结果：状态显示“内存骨架”，常见目录在 0.5 秒内显示；导航过程中不启动扫描进度，返回上级同样秒开。

## 18. 局部刷新与骨架内存

1. 记录某个子目录及其父目录显示的大小。
2. 在子目录中增加文件，点击“重新扫描当前目录”。
3. 返回父目录，检查大小是否同步更新。
4. 查看底部“导航骨架”目录数、内存和完整/精简状态。

预期结果：子目录和全部祖先大小同步变化；骨架不写入数据库；超过预算时只精简文件明细，目录仍可导航。

## 19. 多日趋势

1. 分别切换“24 小时、7 天、30 天”。
2. 将鼠标移动到趋势线、灰色会话竖线附近。
3. 检查两个相邻采样相隔超过 5 分钟的区域。

预期结果：范围切换不卡顿；悬停显示精确时间和已用量或会话开始/结束；采样断层显示橙色阴影且趋势线不跨断层连接。

## 20. 界面与关闭回归

1. 将窗口缩小到允许的最小尺寸 900×680，并切换三个标签页。
2. 悬停 Treemap 和增长树的长路径项目。
3. 分别在动画期间执行快速关闭和完整关闭。

预期结果：按钮、趋势、列表和全局状态均可见；长路径可查看；关闭无 Tcl 回调错误，程序退出后无后台残留进程。

## 21. 基线手动切换

1. 先对同一路径完成一次“完整保存”，重新启动程序。
2. 在“空间变化”页来回切换“本次启动快照”和“最近完整保存”。
3. 点击“恢复默认基线”，再手动切换一次。
4. 快速退出并重启，确认仍可选择更早的同路径完整保存，但默认采用本次启动快照。

预期结果：切换立即刷新文件变化和比较时间，不触发扫描、不新增快照；默认规则不因手动切换而改变。

## 22. 运行期间增长对账

1. 启动后制造一段磁盘增长，等待顶部“本次启动后变化”刷新。
2. 打开“空间变化”，核对“本次运行期间磁盘变化（磁盘口径）”。
3. 切到“本次启动快照”，点击“重新扫描当前目录”。

预期结果：顶部和运行期间行使用同一磁盘口径；扫描后文件变化树提供当前监控路径内的定位结果。文件逻辑大小与磁盘已用量允许有差异，三行不得相加。

## 23. 任意两条快照对比

1. 在“快照历史”中普通单击两条不相邻但路径相同的记录。
2. 点击“对比所选两项”。
3. 再选择不同路径的两条记录。

预期结果：同路径任意两项可比较，程序自动按时间确定旧到新并显示相隔时长；不同路径禁用比较并说明原因。

## 24. 故障日志与扫描收尾

1. 正常完成一次扫描后检查 `%LOCALAPPDATA%\DiskGrowthMonitor\ui.log`。
2. 选择“完整保存”并等待程序退出，再次检查日志末尾。
3. 若界面出现空白或无反应，记录状态栏提示并保留该日志用于反馈。

预期结果：日志包含 `baseline_applied`、非零 `treemap_rendered rectangles=`、`scan_ui_finished role=baseline`，完整关闭还包含 `scan_ui_finished role=closing outcome=success snapshot_id=<非零编号>`；界面绘制异常不会删除已完成的扫描快照，也不会让扫描按钮永久不可用。
