# C 盘空间增长监控器 — 优化建议（v0.2.0 审查）

> 基于 0.2.0 版本完整代码审查（scanner.py / storage.py / ui.py / models.py / service.py / tests）。
> 按性价比排序：P0 = 改动小、价值高；P1 = 建议；P2 = 可选。

---

## 🔴 P0 — 强烈建议

### 1. 增长列表的目录项支持双击钻取

- **现状**：`ui.py` 的 `_select_growth_item` 只把路径显示在详情栏（`detail_var`），用户看到增长项后要手动复制路径再去扫描，增长定位链路断了一环。
- **建议**：目录项（kind == "directory"）双击 → 直接以该目录为路径发起扫描。可复用 treemap 的 `_open_rectangle` 逻辑（`ui.py:698-702`）。
- **理由**：这是「看到增长 → 一步定位到具体文件」闭环的最后一块，改动约 10 行。

### 2. 单实例锁

- **现状**：双开两个实例会各建一个 active 会话（`storage.start_session`），下次启动 `recover_active_sessions` 会把对方的会话补记为 interrupted，互相污染数据。
- **建议**：启动时检测（mutex 或 `%LOCALAPPDATA%\DiskGrowthMonitor\app.lock` 文件锁），已有实例则提示并退出，或激活已有窗口。
- **理由**：用户挂着监控时很容易忘记再双击一次。

### 3. 快照表增加保留策略

- **现状**：`disk_samples` 有 30 天清理（`prune_disk_samples`），但 `snapshots` / `snapshot_items` / `session_growth_items` 只增不删。
- **建议**：保留最近 N 天（如 90 天）或最近 N 个快照，超期删除（`snapshot_items` 靠 `ON DELETE CASCADE` 联动）。
- **理由**：当前量级小，但手动扫描频繁后会持续累积，越早加越省事。

---

## 🟡 P1 — 建议（贴合用户使用场景）

### 4. 开机自启选项

- **建议**：设置区加「开机自启」开关（写 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 注册表键，路径取 `sys.executable` 或打包后的 exe 路径）。
- **理由**：用户使用模式就是「开机建档 → 日常挂着 → 关机对比」，手动启动是唯一麻烦点。契合会话设计。

### 5. 增加「减少视图 / 净变化」

- **现状**：`compare_snapshots` 只报增长（`new.size_bytes > COALESCE(old.size_bytes, 0)`），用户清理 C 盘后看到「未发现增长项目」会以为工具没干活。
- **建议**：
  - 顶部「本次启动后变化」为负值时用绿色/蓝色高亮显示；
  - 增长列表加「减少」排序视图（按 `old - new` 降序，WHERE `new.size_bytes < old.size_bytes`）。
- **理由**：清理后能确认「哪块降了、降了多少」，形成正反馈。

### 6. 关闭时可选「跳过地址快照，快速退出」

- **现状**：C 盘 closing 扫描要几分钟，用户点 X 后只能干等（`_begin_close_snapshot` 无条件走完整路径）。
- **建议**：关闭时弹小确认框：「完整保存（推荐）/ 仅保存总增长快速退出」；快速退出走 `_finish_without_address_snapshot`（已有），下次启动可提示补扫。
- **理由**：用户急着关机时不想等，默认仍走完整路径即可。

### 7. 启动路径无效自动回退

- **现状**：`_start_scan` 里 `os.path.isdir` 失败直接 return（`ui.py:327-329`），`baseline_pending` 保持 True 但无任何重试/提示，会话静默降级为无地址基线。
- **建议**：路径无效时回退 `C:\` 并提示，或至少状态栏提示「基线未建立，关闭时将只保存总增长」。

---

## 🟢 P2 — 可选

### 8. 容量口径 vs 文件口径标注

- 顶部「本次启动后变化」是磁盘容量（`used_bytes`，含 NTFS 簇/压缩/硬链接因素），增长明细是文件逻辑大小之和，两者数值对不上会让用户困惑。
- 建议 UI 标注「磁盘口径 / 文件口径」，或在明细区加一行说明。

### 9. 深层增长定位提示

- 二级目录总大小涨了、但明细里没命中具体文件时（说明增长在深度 > 2 的深层），在列表该行标注「深层有增长，建议钻取」。
- 引导用户使用 P0-1 的新钻取功能，减少「明明涨了却找不到」的挫败感。

### 10. 托盘常驻 + 异常增长通知（大改动，可放下一版本）

- 本次会话增长超过阈值（如 1GB）时托盘气泡提醒；
- 需要把「关窗口 = 退出」改为「关窗口 = 最小化托盘」；
- 涉及生命周期重构（`_on_close` / `atexit` / `_finalize_on_process_exit`），建议单独排期。

---

## 备注

- 测试现状：9 项全过（8 通过 + 1 跳过符号链接），新增建议若涉及 storage 层，建议按现有 `tests/test_storage.py` 风格补测试。
- 上述建议均未改变「只读分析、不删除任何文件」的产品边界。
