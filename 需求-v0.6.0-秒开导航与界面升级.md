# 功能需求：秒开导航 + 多日趋势 + 界面平滑 + 信息完善（v0.5.0 → v0.6.0）

> 提交人：用户实测反馈（基于 v0.5.0）
> 状态：已实现，待用户验收

---

## 一、秒开导航：目录骨架驻留内存（零重扫）

### 1.1 现状问题

- 扫描时遍历了全部文件，但只保存浅层 2 级 + top 200 大文件（深度截断策略），**深层目录明细被丢弃**。
- 因此双击进入深层目录（如 `C:\Users\ranhejian\AppData`）时没有现成数据，需要重新扫描，等待数秒～数分钟。
- 目标体验参考 SpaceSniffer：**一次扫描，全树可导航，进入任意层级秒开，零重扫**。

### 1.2 方案：扫描时构建「目录骨架」驻留内存

扫描遍历逻辑不变（本来就要遍历全部），但额外收集**完整目录骨架**，保存在内存（会话级）：

```
目录骨架（内存 dict）：
    path -> 目录汇总 + 直接子项摘要
        直接子目录：全部保留
        直接文件：每目录 Top N，且受全局条目预算限制
        其余文件：聚合为“其他文件”（总大小 + 文件数）
```

- **覆盖范围**：全树所有目录（不再受 record_depth 限制）。
- **文件明细**：treemap 显示每目录 Top N 大文件，其余小文件合并为“其他文件”；数据库仍保留原有全局 top 200。
- **DB 快照策略完全不变**：仍只存浅层 2 级 + top 200（90 天保留无忧，不膨胀）。

### 1.3 内存约束

- Python 对象实际占用高于裸字段估算，不能按每条 60-100 字节计算。
- 采用紧凑目录节点、每目录文件上限和全局文件条目预算。
- 运行时估算骨架占用；超过 **50 MB** 时自动移除单文件明细，只保留全部目录和“其他文件”聚合。
- 验收同时记录实际进程内存增量，避免只相信理论估算。

### 1.4 导航行为

- 双击/面包屑/↑ 上一级进入任意目录 → 优先查**内存骨架** → 直接渲染子项 treemap（秒开）。
- 骨架未命中（如会话重启后）→ 回退现有逻辑：nav_cache → DB 历史快照 → 重新扫描（v0.4.0 已有）。
- 「重新扫描当前目录」= 刷新该目录及后代的骨架数据，并把大小、文件数、目录数差值逐级传播到全部祖先。
- 数据新鲜度：骨架是扫描时点快照，长时间挂着可能与磁盘有出入，重新扫描即刷新（与现有缓存语义一致，状态栏标注数据时间）。

### 1.5 实现注意

- `ScanResult` 或独立数据结构携带骨架；扫描线程组装，主线程只读。
- 扫描线程完成后一次性交付骨架；本版不共享扫描中的可变字典。
- 内存清理：会话结束/退出时释放；不落库。

---

## 二、多日趋势（① 空间分布页趋势图升级）

### 2.1 时间范围切换
- 趋势图提供范围切换：**24 小时 / 7 天 / 30 天**。
- 数据源：`disk_samples` 表（现有 30 天保留策略已覆盖）。
- 切换控件：trend 面板右上角小按钮组（或下拉），选中态高亮。

### 2.2 会话边界标记
- 从 `monitor_sessions` 读取 `started_at` / `ended_at`（当前 drive，范围内），在趋势线上画**小竖线**标记会话边界。
- 竖线颜色区别于趋势线（如浅灰），hover 显示"会话开始/结束"。

### 2.3 盲区缺口阴影
- 采样时间序列存在断层（相邻采样间隔 > 阈值，如 1 小时）时，断层区间画**橙色半透明阴影**。
- 与 v0.5.0 盲区提示语义一致（期间未监控）；趋势图断层判定独立于 100MB 容量阈值。

### 2.4 悬停显示数值
- canvas `<Motion>`：鼠标接近趋势线时显示最近数据点的时间 + 已用量（浮动小标签或状态栏提示），离开画布隐藏。

### 2.5 性能注意
- 30 天 ≈ 4.3 万条采样，超过 ~2000 点**抽样绘制**（等间隔取点），悬停按原始数据精确匹配。

---

## 三、界面平滑（克制实现）

### 3.1 treemap 布局过渡动画（重点）
- 触发时机：扫描完成、导航切换（进入/返回目录）。
- 实现：记录当前矩形坐标 → 计算目标坐标 → `root.after` 驱动约 12 帧插值（约 200ms 缓动）。
- 窗口 resize 使用 150ms 防抖后重排，不在拖动过程中连续动画。
- 大目录树自动降级为直接绘制（保护主线程）。
- 动画及 resize 回调必须保存 ID，关闭时统一取消。

### 3.2 延后项目

- “边扫边生长”会改变扫描线程消息协议，延后至 v0.6.1 单独评估。
- 指标卡数字滚动不进入本版，避免显示不存在的中间采样值。
- ttk 标签页不模拟透明淡入，避免闪烁和额外回调风险。

### 3.3 按钮悬停反馈
- 主要按钮 `<Enter>/<Leave>` 绑定，hover 时轻微变色/描边（幅度克制，比常态深 10% 左右）。

### 3.4 treemap 悬停高亮 + 提示条
- `<Motion>` 悬停矩形：描边加亮 + 状态栏/浮动条显示 `路径 · 大小 · 文件数`；离开还原。（保留单击显示详情行为。）

---

## 四、信息完善

### 4.1 指标卡
- 可用空间卡：`57.3 GB` → `57.3 GB · 28%`（占总量百分比）。
- 本次启动变化卡：数值旁加小字说明（"相对启动时"）。

### 4.2 增长树（② 页）
- 悬停显示完整路径；项目名过长中间省略（`C:\Users\…\AppData`），列宽自适应。

### 4.3 状态栏
- 补全：当前基线（时间 + 来源：上次完整保存 / 本次启动 / 手动指定）、扫描统计（文件/目录/错误/用时/数据时间）、盲区状态摘要。

### 4.4 快照历史（③ 页）
- 来源显示中文标签：完整关闭 / 启动基线 / 手动保存 / 普通扫描 / 导航扫描。

### 4.5 空状态引导
- 各标签页首次使用/无数据时显示引导文案。

---

## 五、回归注意

- 现有 29 项测试结果不得退化；新增逻辑补测试（骨架结构、局部刷新传播、抽样绘制、断层检测、内存降级）。
- 导航缓存、连续基线、盲区提示、标签页结构、关闭行为等 v0.5.0 行为**全部不变**。
- 动画全部在 tkinter 主线程驱动；关闭时取消全部新增 `after` 回调；性能降级路径必须存在。
- 骨架内存上限 50MB 为硬约束，超出自动降级（只存目录子项）。

## 六、验收标准

1. **秒开导航**：骨架命中的深层导航不启动扫描线程，常见目录 ≤ 0.5 秒完成渲染；↑ 返回同样秒开。
2. 会话重启后导航回退逻辑正常（历史快照/重扫兜底）。
3. 趋势图 24h/7d/30d 切换正常，30 天视图无卡顿（抽样生效）。
4. 趋势图显示会话边界竖线 + 盲区断层橙色阴影 + 悬停数值。
5. 扫描完成/导航切换时 treemap 平滑过渡（约 200ms），大目录直接绘制；resize 防抖生效。
6. 悬停 treemap 矩形显示路径/大小/文件数；按钮使用原生悬停反馈。
7. 可用空间显示百分比；增长树悬停完整路径、长名省略。
8. 状态栏显示基线、扫描统计、数据时间、骨架内存与盲区摘要；快照历史来源中文标签。
9. 完整关闭、快速关闭均无 Tcl 回调错误和后台残留进程。
10. 回归：原有行为不变，新增测试全部通过；权限导致的符号链接跳过须单独说明。
