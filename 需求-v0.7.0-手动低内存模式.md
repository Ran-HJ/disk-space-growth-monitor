# 功能需求：v0.7.0 手动低内存模式（修订版）

> 提交人：Codex 版本规划建议（2026-08-19），用户确认
> 评审：Codex 边界评审（2026-08-19），5 点修正已并入
> 前置：v0.6.3（关闭日志补丁）验收通过后启动本版本
> 状态：可开发

---

## 一、背景与目标

### 背景

- 真实 C 盘扫描实测：窗口进程峰值 **185.4 MB**，两进程合计 195.7 MB。
- 笔记本（16GB）打游戏时需要省内存；运行模式将成为后续 CLI / 自动切换的基础数据模型，因此**先做稳状态转换和数据语义**。
- 第一版**只做手动切换**：不做游戏自动检测、不做托盘、不做启动参数。

### 目标

1. 手动在「全功能模式」与「低内存模式」间切换，切换安全、数据语义正确。
2. 低内存模式内存显著下降（**先测量真实下限再定目标，不预先承诺 <50MB**）。
3. 为 v0.7.2 自动切换 / v0.7.1 CLI 的 `mode` 概念打好基础。

---

## 二、模式定义

### 全功能模式（默认，现状行为不变）

- 一切现有功能：自动基线扫描、目录骨架、导航缓存、增长对比、快照历史、关闭对话框。

### 低内存模式

| 保留 | 停止/释放 |
|---|---|
| 分钟容量采样（disk_samples） | 自动基线扫描 |
| 24h/7d/30d 趋势图 | 快照扫描（手动/导航/关闭） |
| 顶部指标卡（磁盘口径） | 导航骨架 `navigation_skeleton` |
| 启动盲区检测（磁盘口径） | 导航缓存 `nav_cache`、矩形图数据 |
| 快照历史查看与**历史快照对比**（只读 DB） | 实时增长/减少对比（暂停） |

- **关闭语义**：低内存模式下关闭 = 快速退出语义（`_finish_without_address_snapshot("low_memory_close")`），不弹关闭对话框、不扫关闭快照。
- **UI 呈现**：
  - ① 空间分布页：treemap 区域显示占位「低内存模式：未扫描，切回全功能后可补扫」；趋势图仍显示。
  - ② 空间变化页：只显示磁盘口径行（本次运行期间 + 启动盲区），文件快照行显示「低内存模式：无实时快照对比」。
  - ③ 快照历史页：可查看；**历史快照对比仍可用**，但切换到 ② 页显示对比结果时，必须标注**「历史快照对比，不代表本次低内存期间变化」**（评审 #4：低内存期间无实时文件定位，历史对比是历史数据，不能误导为当前期间变化）。
  - 状态栏显示当前模式。

---

## 三、切换语义（核心状态机）

### 3.1 全功能 → 低内存

- 若扫描进行中：**提示等待扫描结束或取消后再切换**（不允许扫描中切换，避免并发数据竞争）。
- 切换动作：
  1. 释放 `navigation_skeleton`、`nav_cache`、`rectangle_items` 等内存结构；
  2. **记录低内存切换上下文**（评审 #1，用于切回时单独计算，不覆盖启动盲区）：
     - `low_memory_started_at`（切换时间）
     - `low_memory_start_sample`（切换时的磁盘采样）
     - `low_memory_reference_snapshot_id`（进入低内存前最近一次已完成的同路径快照）
     - 参考快照时间
  3. 保留会话状态（`session_id`、`session_start_snapshot_id`、`session_start_sample` 不变）。
- 模式选择持久化到 `app_settings`（key：`run_mode`，值 `full` / `low_memory`），下次启动沿用上次模式。

### 3.2 低内存 → 全功能（补扫询问三选一，评审 #3）

**情况 A：本会话由全功能切入低内存（存在低内存切换上下文）**

询问用户：「切回全功能，是否补扫并定位低内存期间的变化？」

| 选择 | 行为 |
|---|---|
| **是** | 切回全功能 → 立即扫描当前监控路径 → 重建骨架 → 与**补扫参考快照**对比 |
| **否** | 切回全功能，**不扫描**；treemap 显示「数据尚未重建」，扫描按钮恢复可用（用户可随时手动扫描） |
| **取消** | **保持低内存模式不变** |

- 补扫参考快照优先级（评审 #2）：
  ```
  automatic_current_snapshot_id（进入低内存前最近一次已完成的同路径快照）
  → session_start_snapshot_id（会话启动快照）
  → 无可用参考
  ```
- **诚实表述**（评审 #2）：若参考快照时间早于 `low_memory_started_at`，对比结果界面必须写**「自参考快照（时间）以来」**，不能声称全部变化都发生在低内存期间。
- 无可用参考时：补扫只建立新基线，提示「此前无同路径快照，本次建立新基线」。

**情况 B：程序直接以低内存模式启动（上次退出时为低内存）**

- **初始化低内存上下文**（与情况 A 统一，评审 #1 修正）：
  - `low_memory_started_at = 会话启动时间`
  - `low_memory_start_sample = session_start_sample`
  - `low_memory_reference_snapshot_id = None`
- 切回全功能时：
  - 按 **3.3 节单独计算「低内存期间磁盘变化」**（当前采样 − `low_memory_start_sample`），与启动盲区分开；
  - **建立新的文件基线**（启动自动扫描），并明确提示：「此前只有磁盘口径记录，无文件地址明细；本次建立新基线」。
- **启动盲区定义不变**（上次程序结束 → 本次程序启动，启动时计算一次）；冷启动低内存时，从本次启动到切回前的区间**不属于启动盲区**，归入低内存期间变化。

### 3.3 低内存期间磁盘变化（与启动盲区分离，评审 #1）

- 切回全功能时，**单独计算**「低内存期间磁盘变化」= 当前采样 − `low_memory_start_sample`（会话内区间）。
- 该数值与启动盲区（上次程序结束采样 → 本次启动采样）**分开显示、分开标注**，不得混用：
  - 启动盲区：跨会话间隙（v0.5.0 既有逻辑，不变）。
  - 低内存期间：本会话内切换区间（新增）。
- 会话连续性：低内存期间会话仍为 `active`，分钟采样照常落库。

---

## 四、内存测量（开发前置步骤，评审 #5）

1. **指标**：同时记录 `WorkingSet64` 与 `PrivateMemorySize64`；分别统计窗口进程与单文件 EXE 两进程**合计**。
2. **场景**：
   - 场景 1：低内存模式**冷启动**（直接以低内存模式启动）的稳定值。
   - 场景 2：全功能扫描完成后 → 手动切到低内存 → 稳定值。
   - 场景 3（对照）：全功能模式扫描完成后的稳定值。
3. **注意**：Python 释放对象后**工作集不一定立即下降**（GC 延迟、内存池缓存）；每个场景记录 5 分钟稳定值，不只看瞬时值；必要时两次 GC/等待后再采样。
4. 基于实测设定 v0.7.0 低内存目标（写进 README，不预先承诺 50MB）。

---

## 五、实现注意

- 模式状态集中管理（如 `self.run_mode`），UI 控件状态随模式切换（扫描按钮禁用、标签页占位等）。
- 释放数据结构时注意：`nav_invalidated_roots`、`change_context` 等关联状态一并处理，避免切回后引用失效。
- 低内存关闭走 `low_memory_close` end_reason，区别于 `quick_close`（便于日志/历史区分）。
- 低内存期间③页历史对比可用；切换到 ② 页展示历史对比结果时必须带「历史快照对比，不代表本次低内存期间变化」标注（评审 #4）。
- 切换操作全程主线程执行（释放内存结构很快，无耗时操作）；扫描中的切换限制见 3.1。

## 六、回归注意

- **现有 39 项测试不得退化**；新增测试后总套件结果 OK；符号链接权限跳过单独说明（评审 #5）。
- 新增测试覆盖：
  - 模式持久化（app_settings round-trip）；
  - 切换状态机（全功能→低内存→全功能，含扫描中拒绝切换）；
  - 低内存关闭 = `low_memory_close` 且无快照；
  - 低内存启动 → 切回 → 新基线 + 启动盲区与低内存期间变化分别提示；
  - 补扫询问三选一（是/否/取消）；
  - 低内存期间磁盘变化与启动盲区分开计算（评审 #1）；
  - 补扫参考快照优先级与"自参考快照以来"表述（评审 #2）；
  - 历史对比标注（评审 #4）。
- 全功能模式行为与 v0.6.3 完全一致（回归测试覆盖）。

## 七、验收标准

1. 手动切低内存后：无扫描发生、骨架/导航缓存释放、内存较全功能显著下降（以第四节实测值为基准）。
2. 低内存下：分钟采样与趋势正常；①页 treemap 占位；②页仅磁盘口径；③页可查看，历史对比带「不代表本次低内存期间变化」标注。
3. 低内存下关闭：不弹对话框、快速退出、会话 `completed:low_memory_close`、无残留进程。
4. 全功能→低内存→切回：
   - 询问补扫：选"是"→ 扫描并按参考快照对比（"自参考快照以来"表述）；选"否"→ 切回不扫描、treemap 显示「数据尚未重建」；取消 → 保持低内存。
   - 「低内存期间磁盘变化」单独显示，与启动盲区不混。
5. 低内存启动→切回：建立新基线，提示“此前仅磁盘口径”；启动盲区保持原定义，启动后的低内存区间单独显示。
6. 模式选择持久化：重启后沿用上次模式。
7. 内存测量：三种场景（冷启动/切换后/全功能对照）数据记录在案，README 标注目标值。
8. 测试：现有 39 项不退化，新增用例后总套件 OK；符号链接跳过单独说明。
